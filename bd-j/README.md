Run `Enable BD-J` for FW 13.00 only in Vue after JB for one time. 
And burn `henloader-1300-only.iso` to a BD disc. 
Then this BD-J method could be a backup and offline method for your PS4 with FW 13.00.